import 'package:flutter/material.dart';

class ProductTodayCard extends StatelessWidget {
  
  const ProductTodayCard({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}